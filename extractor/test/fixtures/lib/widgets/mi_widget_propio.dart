import 'package:flutter/material.dart';

class MiWidgetPropio extends StatelessWidget {
  const MiWidgetPropio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.star);
  }
}
